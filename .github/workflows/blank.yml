name: "USENIX-AE: Mamba-DoRA-Fixed"
on:
  workflow_dispatch:

jobs:
  research-marathon:
    runs-on: macos-15  # Using 15 for better stability with current MLX
    timeout-minutes: 355
    steps:
      - name: 1. Clone Research Artifact
        uses: actions/checkout@v4

      - name: 2. Set up Environment
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install "mlx-lm>=0.19.0" datasets huggingface_hub
          mkdir -p data/big_vul adapters_dora results

      - name: 3. Extract & Pre-Filter Data (The "OOM Shield")
        run: |
          source venv/bin/activate
          python3 -c "
          from datasets import load_dataset
          import json, os, random
          try:
              print('🛰️ Pulling Big-Vul...')
              ds = load_dataset('MickyMike/cvefixes_bigvul', split='train', streaming=True)
              buffer = []
              # We only take snippets that fit within a 512-token window (~2000 chars)
              # This prevents the 'malloc' error during tokenization.
              for entry in ds.take(10000):
                  v = entry.get('source', '')
                  f = entry.get('target', '')
                  if v and f and len(v) < 1800 and len(f) < 1800:
                      text = f'### VULNERABLE:\n{v}\n\n### FIXED:\n{f}'
                      buffer.append({'text': text})
                  if len(buffer) >= 3000: break # Sufficient for a 1000-iter run
              
              random.shuffle(buffer)
              with open('data/big_vul/train.jsonl', 'w') as f:
                  for line in buffer: f.write(json.dumps(line) + '\n')
              os.system('cp data/big_vul/train.jsonl data/big_vul/valid.jsonl')
              print(f'✅ Prepared {len(buffer)} filtered pairs.')
          except Exception as e:
              print(f'❌ Error: {e}'); exit(1)
          "

      - name: 5. Execute DoRA Training (Memory Restricted)
        run: |
          source venv/bin/activate
          set -o pipefail
          # MLX_MAX_MB limits the cache to leave room for the OS (14GB total)
          export MLX_MAX_MB=9000
          python -m mlx_lm lora \
            --model mlx-community/mamba-130m-hf-bf16 \
            --train \
            --fine-tune-type dora \
            --data data/big_vul \
            --iters 1000 \
            --save-every 500 \
            --batch-size 1 \
            --max-seq-length 512 \
            --adapter-path adapters_dora 2>&1 | tee results/dora_train_log.txt

      - name: 6. Automated Audit Test
        if: success()
        run: |
          source venv/bin/activate
          python -m mlx_lm generate \
            --model mlx-community/mamba-130m-hf-bf16 \
            --adapter-path adapters_dora \
            --prompt '### VULNERABLE:\nvoid main() { char buf[8]; strcpy(buf, input); }\n\n### FIXED:\n' \
            --max-tokens 100 > results/repair_test.txt
          cat results/repair_test.txt

      - name: 7. Save & Upload
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dora-mamba-results
          path: |
            results/
            adapters_dora/
